{
	"name": "hellopoetryback",
	"main": "Server.ts",
	"type": "module",
	"private": true,
	"prisma": {
		"seed": "bun prisma/seed.ts"
	},
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@playwright/test": "^1.62.0",
		"@types/bun": "^1.3.14",
		"@types/jsonwebtoken": "^9.0.10",
		"@types/node": "^26.1.2",
		"@types/pg": "^8.20.0",
		"cloc": "^2.6.0-cloc",
		"dependency-cruiser": "^18.1.0",
		"eslint": "^10.8.0",
		"eslint-config-prettier": "^10.1.8",
		"eslint-plugin-prettier": "^5.5.6",
		"globals": "^17.8.0",
		"jiti": "^2.7.0",
		"madge": "^8.0.0",
		"prisma": "^7.9.1",
		"tsx": "^4.23.1",
		"typescript": "^6.0.3",
		"typescript-eslint": "^8.65.0"
	},
	"scripts": {
		"dev": "cross-env NODE_ENV=development bun --watch src/Server.ts",
		"start": "cross-env NODE_ENV=production bun dist/Server.js",
		"start:dev": "cross-env NODE_ENV=development bun dist/Server.js",
		"build": "bun build src/Server.ts --target bun --outdir dist",
		"test": "bun run test:all",
		"test:prepare": "bun run db:migrate:test",
		"test:run": "cross-env NODE_ENV=test bun test --reporter=junit --reporter-outfile=./bun.xml",
		"test:all": "bun run test:prepare && bun run test:run",
		"test:use-case": "cross-env NODE_ENV=test bun test -t USE-CASE",
		"test:policy": "cross-env NODE_ENV=test bun test -t POLICY --concurrent",
		"test:unit": "cross-env NODE_ENV=test bun test",
		"test:integration": "bun run test:prepare && cross-env NODE_ENV=test bun test -t INTEGRATION",
		"test:e2e": "bun run test:prepare && cross-env NODE_ENV=test playwright test",
		"check:e2e": "bun run check:env:test && bun run test:e2e",
		"lint": "eslint . --cache",
		"lint:fix": "eslint . --fix --cache",
		"format": "prettier --check . --cache --log-level silent",
		"format:fix": "prettier --write . --cache --log-level silent",
		"fmt": "bun run format",
		"fmt:fix": "bun run format:fix",
		"typecheck": "tsc --noEmit",
		"check:db-isolation": "bun scripts/check-database-isolation.ts",
		"check:env:dev": "cross-env NODE_ENV=development bun scripts/check-env.ts && bun run check:db-isolation",
		"check:env:test": "cross-env NODE_ENV=test bun scripts/check-env.ts && bun run check:db-isolation",
		"check:env:prod": "cross-env NODE_ENV=production bun scripts/check-env.ts && bun run check:db-isolation",
		"check:env": "bun run check:env:dev",
		"barrels": "bunx barrelsby -c barrels.config.json",
		"depcruise": "bunx depcruise src --exclude \"node_modules|dist|coverage|src/generic-subdomains/persistance/prisma/generated\"",
		"metrics:cloc": "cloc src --by-file --exclude-dir=node_modules,dist,build,coverage,prisma --json --out cloc.json --quiet",
		"metrics:depcruise": "depcruise src --exclude \"node_modules|dist|coverage|src/generic-subdomains/persistance/prisma/generated\" --output-type json --output-to depcruise.json",
		"metrics:analysis": "bun architecture-analysis/src/main.ts",
		"metrics": "bun run metrics:cloc && bun run metrics:depcruise && bun run metrics:analysis",
		"check:static": "bunx prisma generate && bun run barrels && bun run lint:fix && bun run format:fix && bun run metrics && bun run depcruise && bun run typecheck",
		"check": "bun run check:env && bun run check:static && bun run test:unit && bun run build && echo 'All checks passed!'",
		"check:test": "bun run check:env:test && bun run check:static && bun run test:unit && bun run build && echo 'All checks passed (test)!'",
		"check:prod": "bun run check:env:prod && bun run check:static && bun run test:unit && bun run build && echo 'All checks passed (prod)!'",
		"check:all": "bun run check:env:test && bun run check:static && bun run build && bun run test && bun run test:e2e && echo 'All backend checks passed!'",
		"db:generate": "npx prisma generate",
		"db:migrate:dev": "cross-env NODE_ENV=development npx prisma migrate dev",
		"db:migrate:test": "cross-env NODE_ENV=test npx prisma migrate deploy --schema src/generic-subdomains/persistance/prisma/schema.prisma",
		"db:migrate:prod": "cross-env NODE_ENV=production npx prisma migrate deploy",
		"db:seed": "cross-env NODE_ENV=development npx prisma db seed",
		"db:reset:dev": "cross-env NODE_ENV=development npx prisma migrate reset --force",
		"db:reset:test": "cross-env NODE_ENV=test npx prisma migrate reset --force --skip-seed --schema src/generic-subdomains/persistance/prisma/schema.prisma",
		"db:reset:prod": "cross-env NODE_ENV=production npx prisma migrate reset --force",
		"db:drop:dev": "cross-env NODE_ENV=development npx prisma db push --force-reset",
		"db:drop:test": "cross-env NODE_ENV=test npx prisma db push --force-reset --schema src/generic-subdomains/persistance/prisma/schema.prisma",
		"db:drop": "bun run db:drop:dev",
		"generate": "bun run db:generate",
		"migratedev": "bun run db:migrate:dev",
		"migrateprod": "bun run db:migrate:prod",
		"seed": "bun run db:seed",
		"reset": "bun run db:reset:dev",
		"resetprod": "bun run db:reset:prod",
		"drop": "bun run db:drop",
		"data:add-test": "bun scripts/add-test-data.ts",
		"add-test-data": "bun run data:add-test",
		"admin:create": "bun scripts/create-admin.ts",
		"admin:create:prod": "cross-env NODE_ENV=production bun scripts/create-admin.ts",
		"create-admin": "bun run admin:create",
		"create-adminprod": "bun run admin:create:prod",
		"create:domain": "bun scripts/domain-generator/cli.ts",
		"create:project-template": "bun scripts/project-template/cli.ts",
		"docker:build": "docker build -t api .",
		"docker:run": "docker run --env-file .env.production -p 5000:5000 api",
		"docker:up": "bun run docker:build && bun run docker:run",
		"compose": "docker-compose up --build"
	},
	"dependencies": {
		"cross-env": "^10.1.0",
		"@aws-sdk/client-s3": "^3.1098.0",
		"@aws-sdk/s3-presigned-post": "^3.1098.0",
		"@aws-sdk/s3-request-presigner": "^3.1098.0",
		"@elysiajs/cors": "^1.4.2",
		"@elysiajs/openapi": "^1.4.15",
		"@prisma/adapter-pg": "^7.9.1",
		"@prisma/client": "^7.9.1",
		"barrelsby": "^2.8.1",
		"bcryptjs": "^3.0.3",
		"dotenv": "^17.4.2",
		"elysia": "^1.4.29",
		"elysia-rate-limit": "^5.1.0",
		"jsonwebtoken": "^9.0.3",
		"kleur": "^4.1.5",
		"pino": "^10.3.1",
		"pino-pretty": "^13.1.3",
		"slugify": "^1.6.9",
		"ts-morph": "^28.0.0",
		"xss": "^1.0.15"
	},
	"overrides": {
		"@aws-sdk/client-s3": "^3.1009.0"
	}
}
